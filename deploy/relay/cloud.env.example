# Non-secret Cloud edition relay discovery. Planet adds per-session tokens.
# APP-510 live alpha endpoint (DigitalOcean SFO3). Replace the IP with the
# stable DNS name after DNS write access is granted and the A record is live.
SOYAOS_RELAY_ENDPOINTS=relay+udp://137.184.35.52:7443
SOYAOS_RELAY_FREE_RATE_MBPS=10
