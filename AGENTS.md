# Agent 开发约定

## 主干开发

- 本仓库采用主干开发模式；本地开发始终在 `main` 上进行，不创建或切换本地功能分支、发布分支或 Agent 专用分支。
- `origin/main` 启用仅允许通过 Pull Request 合并的保护规则，禁止直接推送、强制推送和删除。
- 交付改动时，不切换本地分支；将当前 `HEAD` 推送到临时远端 PR 分支（例如 `git push origin HEAD:refs/heads/<临时分支名>`），创建并合并 PR 后删除该远端分支。
- 不维护任何长期远端分支。
